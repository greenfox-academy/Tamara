# - Create a function called `printer`
#   which prints the input parameters
#   (can have multiple number of arguments)

from print import argument
def printer(argument):
    input = (argument)
   
print(printer(arg))
print("Total score for " + name + " is " + score)